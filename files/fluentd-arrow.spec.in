Name: fluentd-arrow
Version: @@STROM_VERSION@@
Release: @@STROM_RELEASE@@%{?dist}
Summary: Fluentd plugin to write out logs in Apache Arrow format
Group: Applications/File
License: PostgreSQL
URL: https://github.com/heterodb/pg-strom
Source0: @@STROM_TARBALL@@.tar.gz
BuildRoot: %{_tmppath}/%{name}-%{version}-%{release}-root

%description
fluentd-arrow provides ArrowWrite class designed for the Fluentd output plugin.
This package is built from @@PGSTROM_GITHASH@@ of the Git repo.

%prep
%setup -q -n @@STROM_TARBALL@@

%build
%{__make} -C fluentd TD_AGENT=1DESTDIR=%{buildroot}

%install
rm -rf %{buildroot}
%{__make} -C fluentd TD_AGENT=1DESTDIR=%{buildroot} install

%clean
rm -rf %{buildroot}

%files
/opt/td-agent/lib/ruby/*





